import { View, Text } from "react-native";

export default function PlayerScreen() {
  return (
    <View className="flex-1 items-center justify-center bg-white">
      <Text className="text-lg">Player ekranı (placeholder)</Text>
    </View>
  );
}
